import {concept as C} from './helpers.mjs';
export default [
C('proximity-search','附近搜索：先找区域，再算距离','advanced','找出你周围三公里的咖啡店。对全城每家店精确算一次距离很浪费；但只查所在网格，也可能漏掉街对面的店。',[
 ['粗选候选','用地理网格、Geohash 或空间索引缩小候选区域。空间编码让位置变成可查的区域标识。','经纬度>相交网格>候选商户'],
 ['处理边界','查询圆会穿过多个格子，必须覆盖相交区域，再对候选计算精确距离，排除落在圆外的点。','相邻网格>精确距离>三公里内'],
 ['调整粒度','网格粗则候选多，网格细则查询格子多。城市密度不均，固定粒度不一定处处合适。','网格大小>候选数量>查询扇出']
],[['只查用户所在格子安全吗？','可能漏掉相邻格子内的近点','安全，近点都在同格','地理距离与编码边界不是一回事。','用户可能就在网格边缘。'],['粗筛后为什么还算精确距离？','排除区域内但半径外的点','因为索引完全没用','矩形或格网近似通常包含多余候选。','索引已经减少了需要计算的数量。'],['格子越小一定越快吗？','不一定，需要查询更多格子','一定，没有额外成本','候选更少与查询扇出要权衡。','细分会增加区域数量。']],['空间检索通常是候选生成加精确过滤。','边界覆盖影响正确性，粒度影响成本。','距离模型、经纬度顺序和单位必须一致。'],['geo'],['db-indexing']),
C('time-series-databases','时序数据：时间是最常用的坐标','advanced','每台服务器每十秒上报 CPU。你最常看的是“过去一小时的趋势”，而不是随便查一个没有时间范围的数据集。',[
 ['按时间组织','以指标名、标签集合和时间戳表示样本。时间分区帮助限制查询范围，并便于批量淘汰旧数据。','指标＋标签>时间分区>样本'],
 ['降低历史精度','近期保留细粒度，长期保存按分钟或小时聚合的结果。降采样节省空间，却会丢失短峰值细节。','原始样本>降采样>长期趋势'],
 ['控制基数','把 request_id 等几乎唯一的字段放进标签，会制造大量时间序列。高基数可能比样本频率更快耗尽资源。','唯一请求标签>海量序列>内存与索引压力']
],[['时间分区有助于什么？','裁剪查询与清理旧数据','保证所有记录严格同步','时间范围可定位相关分区。','分区方式不保证时钟同步。'],['只存每小时平均会丢掉什么？','短时尖峰','所有长期趋势','平均可能掩盖几秒钟的故障。','聚合正适合观察长期趋势。'],['request_id 适合常规指标标签吗？','通常不适合，基数太高','非常适合，每个请求一条序列','高基数制造大量独立序列。','可用日志或追踪保存请求级细节。']],['时间范围、保留期和降采样共同决定存储。','指标标签要有界，避免无穷扩展序列。','计数器重置、缺失与迟到样本也要定义语义。'],['prometheus'],['data-modeling']),
C('data-structures-for-big-data','大数据结构：允许一点误差，换空间','advanced','爬虫只想知道一个 URL 是否可能见过。保存所有字符串很贵；若能先用很小的结构排除绝大多数没见过的地址呢？',[
 ['Bloom Filter','把元素映射到多个位。查询发现任一位为零，则确定未加入；全部为一，只能说可能加入。这里假设正确插入且不支持删除的标准布隆过滤器。','URL>多个哈希位>可能存在或不存在'],
 ['估算不同用户','HyperLogLog 估计去重数量，用误差换空间；它不能列出具体用户，也不能当精确成员查询。','用户事件>HLL>估计基数'],
 ['追踪热门项','Count-Min Sketch 估计频率，哈希碰撞可能高估。要输出 Top K，仍需维护候选项并理解估计误差。','事件>频率草图>候选Top K']
],[['布隆过滤器说“可能存在”后怎么办？','必要时查真实集合确认','直接当作绝对存在','假阳性可能来自位碰撞。','可能存在不是证明。'],['HLL 能列出所有访客吗？','不能，它估计数量','能，它保存完整集合','紧凑摘要舍弃了成员细节。','数量估计不是集合存储。'],['近似结构何时不合适？','必须精确结算的金额账本','允许误差的访问趋势','结算需要可审计精确事实。','趋势估计可容忍明确误差。']],['先写清允许哪种误差，再选择摘要结构。','成员判断、去重计数、频率估计是不同问题。','近似结果不能默默当作精确业务事实。'],['bloom','hll','cms'],['numbers-to-know']),
C('vector-databases','向量检索：找到相似，还没找到事实','advanced','用户搜索“适合雨天的鞋”，文章写的是“防水徒步鞋”。关键词不完全重合，语义表示可以帮助找到候选。',[
 ['生成向量','用同一兼容模型把文档和查询变成向量。相似度度量需与模型和归一化方式匹配。','文档与查询>embedding>向量空间'],
 ['近似寻找邻居','ANN 索引用召回率换速度与空间。增大搜索范围可能提高召回但增加延迟，需用真实数据评估。','查询向量>ANN候选>重排'],
 ['过滤与验证','按租户、权限和新鲜度过滤，再把原文证据交给回答环节。高相似度不是事实正确或用户有权访问的证明。','候选>权限与重排>原文证据']
],[['不同模型生成的向量可随意混搜吗？','不能假定它们处于同一空间','能，维度相同就一定兼容','向量含义由模型定义，维度相同不够。','相同长度不代表坐标语义相同。'],['ANN 的代价是什么？','可能漏掉真正最近的项','必然比精确搜索更准确','近似检索在召回、速度和内存间取舍。','精确最邻近定义本身不会因近似变更准确。'],['相似度高就能忽略权限吗？','不能，必须过滤授权范围','能，相关内容天然公开','检索与授权是独立要求。','相关不代表允许访问。']],['向量负责相似候选，重排与证据核验继续提高质量。','评估召回率、延迟、过滤和模型更新成本。','模型变更可能需要重新嵌入和重建索引。'],['vector'],['db-indexing']),
C('change-data-capture','CDC：让变化自己往下游走','advanced','订单写入数据库成功，但更新搜索索引失败。每个接口都手工双写，最终总会出现两边不一致。',[
 ['读取提交日志','CDC 捕获已提交的数据变更，再交给下游。它反映行的变化，未必天然包含完整业务事件含义。','数据库提交>变更日志>连接器'],
 ['建立初始快照','已有数据需要快照，之后追上日志。快照与日志必须衔接，既不能漏掉中间变化，也要能处理重复。','一致快照>日志位置>增量追赶'],
 ['消费者重放','以实体标识和版本幂等应用更新，保存消费进度。删除事件与模式变更同样要处理，不能只同步新增。','变更事件>版本检查>下游索引']
],[['CDC 捕获的行变化等于完整业务事件吗？','不一定，业务语义可能需 outbox','一定，一行就是一个业务动作','业务动作可能涉及多行与上下文。','数据库结构不能自动表达全部业务意图。'],['快照完成后随便从最新日志开始可以吗？','不可以，要与快照位置衔接','可以，快照包含未来变化','快照期间可能发生新写入。','从最新跳过会漏掉中间增量。'],['同步器只处理新增够吗？','不够，还要更新与删除','够，索引永远只追加','状态副本需要反映全部相关变化。','忽略删除会保留过时甚至无权限的数据。']],['日志捕获减少业务代码双写的缺口。','快照、日志位置、重放和版本处理构成可靠同步。','CDC仍有延迟；下游不是与主库同步的同一个事务。'],['cdc'],['multi-step-processes']),
C('shopify-inventory-reservations','Shopify：把库存争用拆小','wild','Shopify 的公开工程文章描述了把预留与库存账本统一到 MySQL 的过程。我们用两位买家争抢库存的简化场景，理解它为什么不仅是“换数据库”。',[
 ['先找一致性边界','预留在 Redis、账本在 MySQL，跨库步骤之间可能失败。统一存储让相关状态能在同一事务边界内更新。','Redis预留>跨库间隙>MySQL账本'],
 ['拆分热行','可预留单位形成有界行池，SKIP LOCKED 跳过被锁定单位，让其他请求处理不同单位。池需与账本一致地补充。','买家>未锁单位行>事务预留'],
 ['看真正瓶颈','工程文章还讨论连接占用等限制。以下是教学推演：若CPU不高但请求排队，应检查连接持有与锁等待，而不只升级CPU。','请求排队>连接与锁观测>针对性优化']
],[['把两份相关状态放同库的主要收益？','建立更清晰的事务边界','数据库品牌自动防所有错误','相关更新可原子提交。','正确性仍依赖事务与数据模型。'],['SKIP LOCKED 在这里为什么有用？','不同请求可以领取不同空闲单位','自动为售罄商品创造库存','它减少对同一已锁行的等待。','跳过锁不会创造实际资源。'],['CPU不高就一定还有吞吐空间吗？','不一定，连接或锁可能饱和','一定，CPU是唯一瓶颈','等待资源也会限制整体吞吐。','系统有多种串行和资源上限。']],['实践事实：统一事务边界，并拆分可争用的库存单位。','迁移的价值来自模型与约束，而非产品名字。','课内路径是简化推演，生产细节请核对原文。'],['shopify','tx'],['dealing-with-contention']),
C('discord-messages-scylladb','Discord：换快数据库，也要减少重复读','wild','Discord 公开介绍过从 Cassandra 迁移到 ScyllaDB，以及用数据服务合并重复读取。用一个热门频道的场景，可以分清这两种优化。',[
 ['限制历史分区','频道与时间桶组合定位消息分区，消息标识控制顺序。时间桶限制分区体积，当前热点仍可能集中在最新桶。','频道＋时间桶>消息分区>倒序历史'],
 ['合并相同请求','多个用户同时要同一段消息时，数据服务共享正在进行的查询。这里展示原理，不复现生产吞吐数字。','相同历史请求>请求合并>一次底层查询'],
 ['区分两个收益','存储引擎改善底层执行，合并请求减少送入底层的工作。更快机器不能自动消除无限重复请求。','更少查询>更快存储>更稳的读取']
],[['分时间桶自动解决最新频道热读吗？','不会，最新桶仍然很热','会，所有用户读不同桶','数据容量分散不等于请求分散。','同时在线用户往往读取同一段新消息。'],['请求合并减少什么？','同时进行的重复底层查询','所有历史数据体积','合并在途工作让多个等待者共用结果。','存储数据并没有因此删除。'],['换引擎与请求合并相同吗？','不同，分别改善执行与工作量','相同，都是更换机器','要分别测量查询成本和查询数量。','它们作用在不同层面。']],['实践事实：存储引擎迁移与读请求合并共同改善系统。','把数据增长、热键和引擎成本分开定位。','生产指标取决于工作负载，不能直接套用到自己的系统。'],['discord'],['cassandra','caching']),
C('slack-job-queue','Slack：把积压从内存前移','wild','Slack 的队列曾因下游变慢而积满内存。其工程文章描述在 Redis 调度层前加入 Kafka 缓冲；这里用突发任务演示这一分层。',[
 ['积压留在内存','下游慢于入口，内存队列持续增长。队列管理本身也需要资源，满载可能让恢复更困难。','任务突发>Redis积压>慢Worker'],
 ['增加持久缓冲','Kafka 保存入口积压，再受控地送入 Redis。已有调度语义保留，缓冲与快速分发各自承担职责。','入口>Kafka缓冲>Redis调度>Worker'],
 ['限制搬运速率','教学推演：下游尚未恢复时降低搬运速率，避免只是把磁盘积压快速重新灌进内存。磁盘保留与容量也必须监控。','持久积压>速率控制>有界内存队列']
],[['把积压放持久日志意味着无限容量吗？','不意味着，磁盘与保留期有限','意味着，可以永远不处理','任何存储都有容量与保留边界。','积压持续增长最终仍会耗尽资源。'],['为什么可能保留 Redis 调度层？','复用已有任务调度语义','Kafka只能存图片','缓冲和每任务管理可以分层。','Kafka能保存一般事件字节。'],['磁盘缓冲到内存应怎样搬运？','按下游能力限速','尽快一次性全部灌入','搬运速度应尊重内存与处理容量。','无节制搬运会重现内存故障。']],['实践事实：持久缓冲前置，保留快速调度层。','缓冲买来恢复时间，不解决无限超载。','观察积压年龄和搬运速率，而不仅看任务数。'],['slack','kafka'],['scaling-writes']),
C('figma-multiplayer','Figma：按属性合并，保留即时手感','wild','两个人同时改一个矩形：一个改颜色，一个改宽度。直接以整份文档覆盖，可能丢掉其中一个人的修改。',[
 ['拆到对象属性','Figma 公开设计以对象与属性组织状态。不同属性的修改可以分别合并，不必让一处改动覆盖整个文档。','改颜色＋改宽度>属性级更新>保留两项'],
 ['同属性发生竞争','公开方案用中央服务对同属性写入确定胜者。客户端先乐观显示，再按服务端结果收敛；不等于取客户端时钟最大的写入。','同属性写入>服务端排序>收敛状态'],
 ['结构还有约束','图形文档是一棵对象树，属性收敛还不够，重设父节点必须防环。下面的流程用于解释结构校验，不复刻完整协议。','移动对象>树约束校验>有效结构']
],[['为何不总覆盖整个文档？','会丢失其他属性的并发修改','文档不能发送网络','细粒度更新减少不必要冲突。','网络可以传文档，问题是合并语义。'],['即时显示是否代表服务端已确认？','不是，可能只是乐观本地状态','是，屏幕更新就是提交成功','本地反馈与全局确认是不同阶段。','界面显示不等于网络已完成。'],['属性最终一致就一定是合法树吗？','不一定，还要避免环等结构错误','一定，任何属性组合都合法','局部合并不能自动证明全局结构约束。','父子关系可能形成无效循环。']],['实践事实：属性级合并与中心协调结合。','即时手感、最终收敛与结构合法性分别处理。','图形对象与任意文本序列的冲突模型不同。'],['figma'],['realtime-updates']),
C('spotify-data-lake-point-queries','Spotify：在数据湖里直接找到那几行','wild','用户想查去年夏天听过的歌，数据却在大量 Parquet 文件里。Spotify 的公开方案为点查增加外部索引，减少查找过程中的依赖读取。',[
 ['文件粗筛还不够','按用户分桶能减少候选文件，却仍可能要逐次读取元数据、键列和数据页。每一步等上一步，延迟会叠加。','候选文件>元数据与键>目标数据页'],
 ['预先建立位置索引','外部索引把键映射到文件与行位置，结合缓存页元数据进行范围读取。它改变访问路径，而不是每次扫描整湖。','用户键>文件行位置索引>范围读取'],
 ['布局也有代价','按键聚合或共置需要的列，可以少读字节或少做I/O；但一些布局会让单列分析扫描更贵。教学重点是访问模式间的权衡。','按键组织>较少点查I/O>扫描代价变化']
],[['只减少候选文件，能消灭文件内查找吗？','不能，仍需定位页和行','能，知道文件就知道全部字节位置','文件选择与文件内定位是两层。','大文件内部还有索引与页布局。'],['外部索引记录什么才有帮助？','键到文件与数据位置','只记录文件总数','位置映射能跳过逐步发现过程。','总数无法定位用户数据。'],['更适合点查的布局总利于列扫描吗？','不一定，可能增加单列读取量','一定，所有查询成本同步下降','访问模式不同，物理布局收益不同。','共置字段可能让只读单列时读取额外字节。']],['实践事实：外部位置索引与精确范围读取服务数据湖点查。','预计算定位信息可缩短依赖I/O链。','点查与分析扫描共享数据时，要量化各自代价。'],['spotify'],['db-indexing','large-blobs'])
];
