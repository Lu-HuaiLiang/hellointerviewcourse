import {concept as C} from './helpers.mjs';
export default [
C('redis','Redis：快，也要想清楚会丢什么','tech','你用 Redis 保存排行榜。更新分数很快，但如果它重启了，昨天的比赛结果还能找回来吗？',[
 ['选择数据结构','Sorted Set 让成员关联分数，适合排行榜；Hash 适合对象字段。数据结构与操作模式比“键值库”三个字更有解释力。','玩家得分>有序集合>排名查询'],
 ['内存达到上限','配置的淘汰策略决定是否移除键或拒绝写入。可丢的缓存与不能丢的业务数据不应随意混在同一淘汰域。','内存上限>淘汰策略>删键或拒写'],
 ['节点重启','RDB、AOF 和复制各有恢复与性能代价。异步复制存在丢失最近已确认写入的窗口，不能仅凭“有副本”承诺零丢失。','写入>持久化与复制>故障恢复']
],[['排行榜适合哪类结构？','有序集合 Sorted Set','每次扫描所有字符串','有序集合按分数维护成员顺序。','全扫描会随着玩家增加变贵。'],['缓存淘汰会发生什么？','部分键被移除，需要回源','数据自动永久归档','淘汰释放内存，不是存档。','缓存淘汰不提供归档保证。'],['异步副本一定保留最后一次写入吗？','不一定，复制可能落后','一定，有副本就零丢失','故障发生在复制前就可能丢数据。','副本存在与复制完成不是一回事。']],['先选数据结构，再解释内存、持久化和复制策略。','原子命令不等于任意跨系统事务。','缓存数据必须有清晰的重建方式。'],['redis'],['caching']),
C('elasticsearch','Elasticsearch：先找到包含词的文档','tech','要在百万篇文章里搜“分布式缓存”。每次逐篇查找很慢，倒过来记录“一个词出现在哪些文档”会怎样？',[
 ['建立倒排索引','分析器把文本处理成词项，再建立词项到文档的映射。查询也要用兼容的分析过程。','文档>分析器>词项到文档'],
 ['写入后立即搜索','索引刷新后新文档才能被搜索到；写入确认不等于立即对搜索可见。界面应接受这一短暂差异。','写入确认>refresh>搜索可见'],
 ['修改业务记录','数据库可作为事实来源，通过事件更新搜索索引。索引落后或损坏时可以重建，但要保留同步进度。','业务数据库>变更事件>搜索索引']
],[['倒排索引的方向是什么？','词项到文档','文档到服务器密码','它缩小包含该词的候选集合。','权限资料不是倒排检索的定义。'],['写入成功后必定立即搜得到吗？','不一定，要考虑刷新','一定，确认就是搜索可见','近实时搜索存在可见性延迟。','持久化与搜索刷新是不同环节。'],['搜索索引出错怎样恢复？','从事实来源重建并追赶变更','把所有业务记录也删除','可重建索引降低数据恢复风险。','索引错误不应毁掉原始事实。']],['倒排索引解决文本候选定位。','搜索通常近实时，不等于事务数据库。','分片、相关性、分页和同步延迟都要按查询考虑。'],['elastic'],['db-indexing']),
C('kafka','Kafka：消息读过，还能再读','tech','订单事件既要给统计，也要给通知。若第一个消费者读完就删掉消息，第二个消费者该怎么办？',[
 ['追加到分区','事件追加到分区日志，offset 标识位置。同一分区内有序，不同分区之间没有统一全局顺序。','生产者>分区日志>offset'],
 ['两个消费组','统计和通知各自保存消费位置，互不抢走对方的数据。保留期内可以回退位置重放。','日志>统计组与通知组>独立进度'],
 ['处理后崩溃','业务完成但 offset 尚未提交时，恢复可能再次处理。使用业务幂等，或在支持的事务边界内协调状态。','处理副作用>提交前崩溃>重复投递']
],[['Kafka 默认在哪个范围有序？','一个分区内','任意主题所有分区之间','分区是日志顺序的边界。','跨分区需要额外协调才有全局序。'],['统计和通知想各读一遍，用什么？','不同消费组','同组竞争同一分区','消费组维护独立进度。','同组用于分摊工作而不是各自全量读取。'],['用了 Kafka 就能保证外部扣款仅一次吗？','不能，外部副作用仍需幂等','能，日志自动覆盖银行操作','事务保证有明确边界。','消息中间件无法自动纳入任意外部系统。']],['分区日志、消费组、offset 是三个关键概念。','保留期限定能否重放；磁盘也不是无限的。','投递语义与外部业务副作用分开处理。'],['kafka'],['patterns']),
C('api-gateway','API Gateway：入口该管什么？','tech','同一套后端服务供手机和网页访问。每个服务都重复实现认证、路由和限流，很容易出现规则不一致。',[
 ['集中入口能力','网关可以统一校验令牌、路由请求和执行粗粒度限流。具体资源是否属于用户，仍需业务服务判断。','客户端>网关>业务服务'],
 ['入口发生故障','单台网关会成为故障点。多个实例搭配负载均衡和健康检查，让新请求避开坏实例。','负载均衡>健康网关>服务'],
 ['避免变得太重','把长事务和业务状态机塞进入口，会耦合所有服务的发布与容量。入口保持边界职责，业务各自演进。','网关>独立业务服务>业务存储']
],[['网关验证登录后还需要什么？','业务资源授权','信任任意请求中的资源ID','登录说明是谁，授权决定能访问什么。','知道ID不等于拥有权限。'],['入口如何避免单点？','多实例与健康路由','只增加一台的内存','冗余实例能在某个进程故障时接替。','更大内存无法消除单点。'],['长订单流程放哪里更合适？','业务服务的状态机','通用路由配置里','业务状态应由明确的业务组件维护。','网关承担长流程会增大耦合。']],['入口统一通用策略，业务服务守住资源权限。','网关本身也需要容量、超时和冗余设计。','重试必须尊重接口幂等性。'],['http'],['api-design']),
C('cassandra','Cassandra：把查询写进主键里','tech','聊天消息写入很密集，常用查询是某频道的一段历史。你希望按已知键读写，而不是随意连接很多张表。',[
 ['分区与排序','partition key 决定数据归属，clustering key 决定分区内顺序。频道加时间桶定位一片，消息序号排列历史。','频道＋时间桶>分区>消息顺序'],
 ['写入与合并','写入经过提交日志和 memtable，之后形成 SSTable。后台 compaction 合并文件；写入便宜不表示读取没有放大。','日志与memtable>SSTable>compaction'],
 ['读取多个副本','一致性级别决定等待多少副本。quorum 有交叠性质，但时钟、并发冲突与故障模型仍影响业务语义。','读取请求>副本集合>协调结果']
],[['哪部分决定分区内顺序？','clustering key','任意返回顺序','聚簇列组织同一分区内的数据。','范围读取需要稳定排列。'],['为什么要限制分区大小？','避免单分区过大与热点','因为每片只能存一条','分区过大会让读写和维护困难。','分区通常承载多行相关数据。'],['quorum 就等于任意业务事务吗？','不是，副本交叠不是通用事务','是，所有表自动串行化','跨实体不变量还需相应协议或建模。','副本数量不能替代事务语义。']],['按访问模式建表，必要时为不同查询冗余视图。','分区键与聚簇键承担不同职责。','关注热分区、墓碑、压缩和读放大。'],['cassandra'],['sharding']),
C('dynamodb','DynamoDB：一次查询应落在哪里？','tech','你要按用户查询订单，同时又要按订单号查详情。先决定键怎样组织，再讨论数据库替你管理多少机器。',[
 ['选择主键','分区键定位数据，排序键表达同一分区内的有序范围。user_id 配合 order_time 可支持用户订单列表。','user_id>分区>order_time'],
 ['新增访问方式','按另一字段查询时可以增加二级索引。全表 Scan 不能当成所有查询的常规替代。','另一查询键>二级索引>候选记录'],
 ['带条件地更新','库存扣减要求旧值足够时才成功。条件写入把检查与修改放在同一原子操作中；事务能力需按实际范围使用。','库存请求>条件检查＋更新>成功或冲突']
],[['新增查询方式先考虑什么？','对应键或二级索引','一直扫描整表','访问路径应和键结构对应。','扫描成本会随全表增大。'],['全局二级索引能承诺强一致读吗？','不能，GSI 读取是最终一致','能，所有读取都一样','读取语义随资源类型和选项变化。','不能把主表的能力泛化到GSI。'],['防并发超卖应怎样扣减？','使用条件更新','先读再无条件写','检查和更新必须原子化。','两个请求可能读到同一个旧库存。']],['托管数据库仍需要良好的访问模式设计。','分区热点不会因“无服务器”自动消失。','区分主表、索引、条件写和事务的能力边界。'],['dynamo'],['data-modeling','sharding']),
C('postgres','PostgreSQL：先把不变量守住','tech','两个人同时转走账户里的最后一笔余额。单条 SQL 很快，但多个操作的交错会不会把余额花两遍？',[
 ['用事务表达整体','相关写入一起提交或回滚，再用唯一、外键和检查约束守住数据规则。事务不是只把语句包起来就万事大吉。','事务开始>约束与写入>提交'],
 ['并发读取旧值','默认 Read Committed 下，不同语句可能看到不同已提交快照。先读后写的业务仍需条件更新或行锁。','读余额>另一事务写入>重新判断'],
 ['处理冲突','更强隔离或版本检查可能导致冲突失败，应用要重试整个事务并限制重试次数。读副本扩读但可能落后。','并发冲突>回滚>有限重试']
],[['把两条语句放事务里就不会竞争吗？','不一定，还要看隔离与锁','一定，事务等于全局串行','隔离级别决定可观察到的交错。','事务原子性不等于所有并发都串行。'],['邮箱唯一性最可靠放在哪里？','数据库唯一约束','只在前端查询一次','约束能覆盖并发与多个客户端。','两次前端检查可能同时通过。'],['序列化失败通常怎样处理？','回滚后重试整个事务','继续提交剩余几条','原事务逻辑要在新快照下重新判断。','残缺执行可能破坏业务不变量。']],['事务、约束、隔离、索引分别解决不同问题。','从正确的单库方案开始，再按证据扩展。','复制不是备份，备份还需验证恢复。'],['tx','pg'],['db-indexing']),
C('flink','Flink：迟到的数据怎么算？','tech','统计每分钟订单，一条 12:00 的事件在 12:02 才到。把它算进 12:02，业务报表就偏了。',[
 ['选择事件时间','按业务发生时间分配窗口，而不是只看处理机器收到事件的时间。事件时间需要来自可信的事件字段。','事件发生时间>分钟窗口>聚合'],
 ['用水位线推进','Watermark 表达事件时间进度的估计，帮助决定何时触发窗口。乱序等待越久，结果越完整但输出越迟。','乱序事件>watermark>窗口输出'],
 ['状态故障恢复','Checkpoint 保存一致的计算状态和来源位置。端到端 exactly-once 还取决于来源可重放与 sink 的事务或幂等能力。','checkpoint>恢复状态>重放输入']
],[['事件时间是什么？','业务发生的时间','服务器当前处理时间','两者在网络延迟时会不同。','处理时间无法自动修正迟到。'],['Watermark 是绝不会再迟到的保证吗？','不是，仍要定义迟到策略','是，之后事件只能丢掉','系统需选择补算、侧输出或丢弃等策略。','水位线是进度机制，迟到仍可能出现。'],['Checkpoint 自动保证任意外部写入仅一次吗？','不能，要看 sink 配合','能，与接收方无关','状态恢复与外部提交需协调。','外部副作用可能已经发生。']],['区分事件时间、处理时间、水位线和窗口。','准确性、等待时间与状态成本一起权衡。','恢复保证必须说明输入和输出的边界。'],['flink'],['kafka']),
C('zookeeper','ZooKeeper：谁现在有资格当领导？','tech','两个调度器都觉得自己是主节点。若它们同时发任务，用户可能收到两次通知。需要一个协调机制来管理资格。',[
 ['记录协调状态','ZooKeeper 适合小规模协调元数据，例如成员、配置和选主信息；不适合当大文件存储或高吞吐业务日志。','节点>协调元数据>成员状态'],
 ['会话与临时节点','临时节点跟随会话生存。会话失效后节点会被移除，但网络中断到被判定失效存在时间窗口。','会话>临时节点>失效清理'],
 ['拦住旧领导','旧进程可能暂停后恢复，仍以为自己持有资格。下游用递增 fencing token 拒绝过期领导的写入。','旧领导>过期token>存储拒绝']
],[['协调服务适合保存什么？','少量成员与选主元数据','所有视频原始字节','协调状态小而重要。','大对象会损害协调服务。'],['客户端断网就立即失去会话吗？','不一定，取决于超时判定','一定，在同一瞬间','故障检测无法瞬间区分慢与死。','网络中断不是同步的全局事件。'],['旧领导恢复后靠什么限制它？','下游校验 fencing token','只让新领导相信自己','约束必须在受保护资源侧执行。','两个进程各自相信自己无法防止旧写入。']],['协调资格不等于能阻止旧进程行动。','会话、超时与fencing共同考虑。','保留协调服务的小数据与低负担特征。'],['zk'],['cap-theorem']),
C('realtime-updates','实时更新：谁主动开口？','pattern','订单状态变了，你希望浏览器尽快显示。是浏览器一直问，还是服务器主动告诉它？',[
 ['定期轮询','客户端每隔一段时间拉取。实现简单，但没有变化时也消耗请求，而且延迟受轮询间隔影响。','浏览器定时>查询状态>返回'],
 ['服务器推送','单向事件流可用 SSE；双向高频交互可以考虑 WebSocket。连接协议不负责自动持久保存历史事件。','服务端事件>SSE或WebSocket>浏览器'],
 ['断线再连','用事件序号或游标从上次位置补齐。若历史保留不足，则重新拉快照；重连使用退避，避免全体同时冲击。','断线>上次游标>补事件或快照']
],[['仅服务器推状态，可考虑什么？','SSE','每个字符新建一个连接','SSE适合服务器到浏览器的事件流。','反复建连浪费固定开销。'],['WebSocket 能保证断线期间不丢历史吗？','不能，需要持久事件与补齐协议','能，长连接会存所有消息','传输通道与历史存储是两层。','断线时通道本身并不存在。'],['大量客户端重连怎么做？','退避并加随机抖动','在同一毫秒无限重试','错开重试降低同步冲击。','同步重试可能加剧故障。']],['按更新方向、频率和延迟需求选择通道。','实时不等于可靠补历史。','序号、快照、心跳和重连策略补齐连接之外的能力。'],['sse'],['networking-essentials']),
C('dealing-with-contention','并发竞争：最后一张票归谁？','pattern','库存只剩 1，两位买家都读到 1，然后各自购买。分别看每个请求都正确，放在一起却卖了两件。',[
 ['先读后写','两个请求读取相同旧值，各自决定有货。若写入没有条件保护，就可能都成功。','A与B读到1>各自扣减>超卖'],
 ['原子条件更新','把“库存大于零”和“扣一”放进同一原子写入；只有成功修改的请求获得库存。','并发请求>条件扣减>一成功一失败'],
 ['竞争越来越热','行锁会串行化热点。可将可分割资源拆成独立单位，或排队限制竞争，但仍需在提交处保证不变量。','热点队列>独立库存单位>事务提交']
],[['先读再写为什么危险？','检查与修改之间可能被并发改变','读操作天生不可靠','中间窗口让两个请求依据同一个旧事实行动。','读本身可以正确，组合仍不原子。'],['条件更新失败意味着什么？','该请求没有拿到这份库存','应该忽略错误继续付款','失败就是竞争或条件不满足，需要返回或重选。','忽略失败会破坏资源约束。'],['分布式锁就不需要数据库保护了吗？','仍要考虑租约与旧持有者','不需要，锁永远有效','租约可能失效，数据库是最后的状态边界。','暂停或网络分区会让旧持有者继续运行。']],['先定义不能被破坏的业务不变量。','条件写、锁、乐观版本和串行化按争用程度选择。','锁的存在不是业务正确性的全部证明。'],['tx'],['postgres']),
C('multi-step-processes','多步骤流程：失败后从哪里接上？','pattern','订单已创建，库存已预留，付款却失败了。把三个服务的接口顺序调用一遍，并不能保证它们一起成功。',[
 ['记录状态机','让订单经过待预留、待付款、已完成等状态，每次转移有条件。状态记录帮助区分没执行和已经执行。','订单>库存预留>付款'],
 ['支付失败','用补偿步骤释放库存。补偿是新的业务操作，不是跨服务按下数据库回滚键；它也可能失败并需要重试。','付款失败>释放预留>已取消'],
 ['消息与写库','在同一事务里写订单和 outbox，再异步转发事件，避免写库成功却没有消息。消费者仍要去重。','订单＋outbox>转发器>消费者']
],[['补偿等同数据库回滚吗？','不等同，它是新操作','等同，所有服务瞬间复原','补偿可能有延迟或不可逆部分。','跨服务没有自动的全局回滚。'],['Outbox 解决哪种缺口？','业务提交成功却没发事件','所有消费者绝不重复','同一事务持久记录待发事件。','转发重试依然可能重复。'],['补偿失败怎么办？','记录状态并重试或人工处理','假装订单已经彻底取消','失败必须可观察和恢复。','隐藏失败会留下占用资源。']],['状态机描述进度，幂等支持重试，补偿修复已做的工作。','Outbox缩小本地业务写入与事件发布的缺口。','向用户区分处理中、完成、失败与待核实。'],['tx','cdc'],['api-design']),
C('scaling-reads','扩展读取：少做，近做，分开做','pattern','一篇文章登上首页，读取暴涨，但正文几乎不变。每次都从主库重新读取，有必要吗？',[
 ['缓存热点','相同结果复用一次计算或查询，减少主库读取。按权限和内容版本组织缓存键，避免串用户或旧数据误用。','读请求>热点缓存>主库'],
 ['使用读副本','不同记录的读取可分到副本。复制延迟意味着刚写的数据可能暂时不可见，关键读可路由主库。','读取>多个读副本>复制追赶'],
 ['预计算结果','复杂统计可异步生成物化视图。读取变轻，写入与刷新变重；需要明确视图的新鲜度。','原始事件>预计算>查询视图']
],[['缓存键漏掉用户权限会怎样？','可能把结果泄露给其他用户','只会影响缓存性能','缓存也是数据访问边界的一部分。','错误复用结果涉及正确性与权限。'],['刚写完立即读不到，可能为什么？','读副本尚未追上','数据一定永久丢失','先区分可见性延迟和数据丢失。','异步复制会造成短暂落后。'],['预计算主要把成本移到哪里？','写入或后台刷新阶段','成本完全消失','提前计算让在线读更便宜。','计算与存储并未免费。']],['缓存消除重复，副本分担读取，视图提前计算。','选择手段前观察热点与查询成本。','始终说明数据新鲜度与权限边界。'],['redis','tx'],['caching']),
C('scaling-writes','扩展写入：队列能买来多少时间？','pattern','传感器每秒送来 1,000 条记录，处理器只能处理 600 条。加上队列后接口不再卡住，但问题真的解决了吗？',[
 ['暂存突发','这组教学数值下，每秒积压增加 400 条，持续 10 秒就是 4,000 条。队列吸收突发，但不能消灭持续速率差。','1000/s到达>队列＋400/s>600/s处理'],
 ['批量处理','合并小写入可摊薄固定开销，但批次等待增加延迟，失败重试范围也更大。批大小要有界。','多条记录>有界批次>存储'],
 ['分区与背压','按键把工作分给多个处理器；下游仍跟不上时限制入口或降级。一个热键的严格顺序可能限制并行度。','分区路由>并行处理>背压反馈']
],[['队列能解决无限持续超载吗？','不能，积压仍不断增长','能，只要使用异步','容量差必须通过扩容或控制入口解决。','异步只是改变等待的位置。'],['批量写有什么代价？','等待批次造成额外延迟','一定提高每条实时性','吞吐与等待时间常需权衡。','组批需要时间。'],['严格同键有序会限制什么？','该键的并行处理空间','任何数据持久化','顺序约束让一部分工作不能任意并行。','持久化仍然可以进行。']],['先量化到达速率、处理速率与积压增长。','组批、分区和异步分别优化不同成本。','监控队列年龄、容量与下游饱和度。'],['kafka'],['sharding']),
C('large-blobs','大文件：字节和元数据分开走','pattern','用户上传一个 2 GB 视频。若所有字节都经过普通应用服务，它很快会被带宽和长连接占满。',[
 ['授权直传','应用服务发放限定对象、时间和权限的上传凭证，客户端直接传到对象存储。服务仍负责鉴权与业务记录。','客户端>对象存储>完成回调'],
 ['分段重试','把文件分成多个 part，单段失败只重传该段；完成时校验各段和整体，合并成对象。','文件>分段上传>校验完成'],
 ['清理与分发','未完成的上传需要清理；可公开分发的内容通过 CDN，私有内容依然校验访问权限。','完整对象>授权或CDN>下载者']
],[['大字节为什么常走直传？','减少应用服务转发负担','绕过所有权限检查','授权与数据路径可以分离。','直传仍须限定凭证权限。'],['分段失败应重传什么？','失败的分段','整个文件必然全部重传','分段协议可独立恢复。','全量重传浪费已成功的数据。'],['没有完成的分段怎么办？','设清理策略回收','永远留着不占空间','残留分段会占用存储与成本。','未完成也是真实存储字节。']],['业务服务处理权限和元数据，对象存储承载字节。','分段、校验、完成状态与清理组成完整上传流程。','CDN不自动解决私有对象授权。'],['s3'],['api-design']),
C('long-running-tasks','长任务：接受请求，不等于做完','pattern','生成一份报告需要五分钟。让一个 HTTP 请求一直等，很容易在网关超时后留下不确定的执行状态。',[
 ['返回任务标识','接受后返回 job_id 和可查询状态，用户知道任务进入队列。此时应展示处理中，而不是报告已完成。','提交>任务记录>返回job_id'],
 ['工人领取租约','Worker 领取任务并记录租约或可见性超时。处理期间续租；失联后任务可以重新领取。','队列>Worker租约>执行'],
 ['完成与恢复','输出以稳定任务标识提交，重试不会重复生成业务结果。失败有重试上限，持续失败进入可观察的失败状态。','幂等输出>标记完成>通知用户']
],[['202 Accepted 通常说明什么？','请求已接受，工作未必完成','所有结果已经生成','接受与执行完成是不同阶段。','状态码不能提前兑现尚未完成的工作。'],['Worker 死掉后任务怎样接回？','租约到期后重新领取','依赖原进程内存','持久记录和租约支持故障恢复。','原进程内存会随故障丢失。'],['重试可能执行两次，应怎样处理？','结果提交保持幂等','假设领取后永不重投','超时与故障可能触发重复。','无法仅凭领取排除执行后的失联。']],['任务标识、持久状态、租约、幂等与失败终态要一起设计。','队列等待与实际执行时间分别可见。','取消需要协作协议，不能只把页面按钮变灰。'],['http','kafka'],['multi-step-processes'])
];
